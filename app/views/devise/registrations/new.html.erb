<div class="container form-style">
  <div class="row justify-content-center align-items-center" style="height:auto">
    <div class="col-6">
      <h2>Регистрация</h2>

      <%= form_for(resource, as: resource_name, url: registration_path(resource_name), class: 'auth_form') do |f| %>
        <%= devise_error_messages! %>

        <div class="form-group">
          <%= f.label :Имя, id: 'placeholder' %><br />
          <%= f.text_field :user_name, autofocus: true, class: 'form-control' %>
        </div>

        <div class="form-group">
          <%= f.label :Адрес , id: 'placeholder'%>
          <%= f.text_area :address, class: 'form-control' %>
        </div>

        <div class="form-group">
          <%= f.label :Номер_телефона , id: 'placeholder'%>
          <%= f.text_field :phone_number, class: 'form-control phone-number'%>
        </div>

        <div class="form-group">
          <%= f.label :Дата_рождения , id: 'placeholder'%>
          <%= f.date_field :date_of_birth, class: 'form-control' %>
        </div>

        <div class="form-group">
          <%= f.label :email , id: 'placeholder'%><br />
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'form-control' %>
        </div>

        <div class="form-group">
          <%= f.label :Пароль , id: 'placeholder'%>
          <% if @minimum_password_length %>
            <em>(<%= @minimum_password_length %> символов минимум)</em>
          <% end %><br />
          <%= f.password_field :password, autocomplete: "off", class: 'form-control' %>
        </div>

        <div class="form-group">
          <%= f.label :Подтверждение_пароля, id: 'placeholder' %><br />
          <%= f.password_field :password_confirmation, autocomplete: "off", class: 'form-control' %>
        </div>

        <div class="form-group">
          <%= f.submit "Зарегестрироваться", class: 'btn btn-custom' %>
        </div>
      <% end %>

      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>